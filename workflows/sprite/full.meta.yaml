# sprite/full — V38 Alpha 5종 동시 출력 (Stage1 + HiRes + Pixelized + PixelAlpha + RembgAlpha).
#
# 워크플로우: Sprite_Illustrious_PoseGuided_Alpha_V38_api_full.json.
# 한 호출에 V38 의 5 출력 모두 — 비교/디자인 결정용.
schema_version: 1

intent: |
  All 5 V38 outputs in one call (Stage1 / HiRes / Pixelized / PixelAlpha /
  RembgAlpha) for comparison and design decision. PixelAlpha is primary.

use_cases:
  - "캐릭터 시트 풀세트 — 일러스트 + 게임 sprite 둘 다 필요"
  - "Cherry-pick UI 후보 풍부화 (한 호출, 5 출력)"
  - "디자인 결정 단계 — 어떤 alpha 가 맞는지 비교 (color vs rembg)"

not_for:
  - "한 종류만 필요 — 시간/VRAM 낭비. 단일 변형 사용 (sprite/pixel_alpha 등)"
  - "fast iteration — use sprite/stage1"

output_layout:
  kind: pose_grid
  rows: 1
  cols: 3
  per_cell_size: [426, 640]
  alpha: true
  notes: |
    5 outputs: Stage1 (raw), HiRes (1920×960), Pixelized (픽셀 정렬), PixelAlpha
    (color-key alpha — primary, 게임 엔진용), RembgAlpha (AI rembg). 각 출력
    저장 라벨은 catalog 의 outputs 필드 참조.

tags:
  - pixel-art
  - sprite-sheet
  - 1x3-grid
  - controlnet-pose
  - all-outputs
  - alpha-pixel
  - alpha-rembg
  - illustrious
  - 게임-스프라이트
  - 풀세트

prompt_template:
  base_positive: |
    pixel_character_sprite, sprite, sprite sheet, (pixel art:1.5), white background,
    1x3 pose grid layout, same character three poses,
    consistent character design, sharp pixel edges, limited palette,
    centered character, no scenery, no background props,
    masterpiece, best quality, very aesthetic
  base_negative: |
    (worst quality, low quality:1.4), blurry, jpeg artifacts, deformed, bad anatomy,
    watermark, text, signature, photorealistic, 3d, realistic, photo,
    smooth shading, anti-aliasing, smooth lines, blurred edges,
    gradient background, complex background, scenery,
    different sizes, varying scale, oversized character, height variation,
    close-up, upper body, portrait, cropped character, headshot,
    floating sword, detached weapon, levitating sword, separated weapon,
    sword in air, weapon hovering, weapon next to character, disembodied weapon
  user_slot:
    label: subject
    description: "pixel_alpha 와 같은 user_slot."
    examples:
      - "1girl, silver hair twin tails, navy school uniform, holding a notebook"
      - "1boy, brown spiky hair, blue tunic, leather belt with sword"
      - "1girl, (black hair:1.2), (side ponytail:1.3), long hair, blue knight armor, (holding silver sword:1.3), sword in right hand, gripping sword tightly, red cape, fantasy warrior"
    required: true
    min_chars: 8
    max_chars: 400
  injection_rule: "{base_positive}, {subject}"

input_labels:
  pose_image:
    description: "ControlNet 포즈 가이드."
    alternatives:
      - pose_grid_1x3_5h_1280x640.png
      - pose_grid_1x4_1280x640.png
      - pose_grid_3x3_1280x896.png
